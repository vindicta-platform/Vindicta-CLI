# Vindicta-CLI Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-07

## Active Technologies

- Python 3.11+ (requires-python >=3.10 per pyproject.toml) + Typer >=0.12, Rich >=13.0 (already declared); new: PyYAML (workspace config), tenacity (retry logic) (001-dev-commands)

## Project Structure

```text
src/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.11+ (requires-python >=3.10 per pyproject.toml): Follow standard conventions

## Recent Changes

- 001-dev-commands: Added Python 3.11+ (requires-python >=3.10 per pyproject.toml) + Typer >=0.12, Rich >=13.0 (already declared); new: PyYAML (workspace config), tenacity (retry logic)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
